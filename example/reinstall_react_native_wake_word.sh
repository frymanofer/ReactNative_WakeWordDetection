npm uninstall react-native-wakeword
npm cache clean --force
npm install react-native-wakeword@latest

while true; do

	# Generate a random number between 10 and 20
	SLEEP_MINUTES=$((10 + RANDOM % 11))

	# Convert minutes to seconds
	SLEEP_SECONDS=$((SLEEP_MINUTES * 10))

	# Sleep for the random duration
	sleep $SLEEP_SECONDS

	npm uninstall react-native-wakeword
	npm cache clean --force
	npm install react-native-wakeword@latest
	# Print "run" to the screen
	echo "run"
	# Generate a random number between 10 and 20
	SLEEP_MINUTES=$((10 + RANDOM % 11))

	# Convert minutes to seconds
	SLEEP_SECONDS=$((SLEEP_MINUTES * 10))

	# Sleep for the random duration
	sleep $SLEEP_SECONDS

	npm uninstall react-native-wakeword
	npm cache clean --force
	npm install react-native-wakeword@latest
	# Print "run" to the screen
	echo "run"
	# Generate a random number between 10 and 20
	SLEEP_MINUTES=$((10 + RANDOM % 11))

	# Convert minutes to seconds
	SLEEP_SECONDS=$((SLEEP_MINUTES * 10))

	# Sleep for the random duration
	sleep $SLEEP_SECONDS

	npm uninstall react-native-wakeword
	npm cache clean --force
	npm install react-native-wakeword@latest
	# Print "run" to the screen
	echo "run"
	# Generate a random number between 10 and 20
	SLEEP_MINUTES=$((10 + RANDOM % 11))

	# Convert minutes to seconds
	SLEEP_SECONDS=$((SLEEP_MINUTES * 10))

	# Sleep for the random duration
	sleep $SLEEP_SECONDS

	npm uninstall react-native-wakeword
	npm cache clean --force
	npm install react-native-wakeword@latest
	# Print "run" to the screen
	echo "run"

done

